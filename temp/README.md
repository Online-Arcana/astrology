# Temporary glyph import drop

Upload exactly one file here on branch `work/viewer-glyph-symbols`:

`astrology_svg_pack_v3.zip`

The temporary `import astrology glyph pack` workflow will validate the archive, copy the discovered pack root to `public/assets/astrology-glyphs/`, delete the ZIP, and commit the imported assets back to this branch.

This directory and the temporary workflow are removed before merge.
